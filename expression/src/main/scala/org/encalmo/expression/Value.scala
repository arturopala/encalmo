package org.encalmo.expression

/**
 * Subtype of Expression representing single value expression.
 * Value eval() should always returns itself.
 * @author artur.opala
 *
 */
trait Value extends Expression